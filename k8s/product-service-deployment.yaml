apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: swiftcart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: swiftcart/product-service:local
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 3002
          envFrom:
            - configMapRef:
                name: swiftcart-config
            - secretRef:
                name: swiftcart-secret
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"