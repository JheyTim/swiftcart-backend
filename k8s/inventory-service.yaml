apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: swiftcart
spec:
  type: ClusterIP
  selector:
    app: inventory-service
  ports:
    - name: http
      protocol: TCP
      port: 3005
      targetPort: 3005