apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: swiftcart
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      protocol: TCP
      port: 3004
      targetPort: 3004