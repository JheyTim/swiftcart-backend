apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: swiftcart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: swiftcart/payment-service:local
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 3006
          envFrom:
            - configMapRef:
                name: swiftcart-config
            - secretRef:
                name: swiftcart-secret
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"